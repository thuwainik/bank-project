import React from "react";
import { NavLink } from "react-router-dom";

const Navbar = () => {
  return (
    <nav>
      <div className="">
        <NavLink></NavLink>
      </div>
    </nav>
  );
};

export default Navbar;
